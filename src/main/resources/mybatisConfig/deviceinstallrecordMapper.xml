<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fastrun.TempCollection.dal.DeviceinstallrecordMapper">

    <resultMap type="com.fastrun.TempCollection.model.Deviceinstallrecord" id="deviceinstallrecordMap">
                  <id column="id" property="id" />
         <result column="PositionID" property="positionID" />
         <result column="DeviceID" property="deviceID" />
         <result column="Installed" property="installed" />
         <result column="WorkState" property="workState" />
         <result column="Status" property="status" />
         <result column="CreaterID" property="createrID" />
         <result column="CreateTime" property="createTime" />
    </resultMap>
    
    <sql id="Base_Column_List">
      `id`,`PositionID`,`DeviceID`,`Installed`,`WorkState`,`Status`,`CreaterID`,`CreateTime`
    </sql>
  
    <select id="getPaging" resultType="com.fastrun.TempCollection.model.Deviceinstallrecord" resultMap="deviceinstallrecordMap">
       SELECT
        <include refid="Base_Column_List" />
       FROM DeviceInstallRecord
       order by ${orderBy} limit #{offset},#{pageSize}
    </select>

    <select id="get" resultType="com.fastrun.TempCollection.model.Deviceinstallrecord" resultMap="deviceinstallrecordMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM DeviceInstallRecord WHERE id=#{id}
    </select>

    <update id="update" parameterType="com.fastrun.TempCollection.model.Deviceinstallrecord"
            flushCache="true"
            statementType="PREPARED"
            timeout="20">
       Update DeviceInstallRecord
        <set>
                   <if test="model.positionID!=null">
                           `PositionID`=#{model.positionID},
                       </if>
        <if test="model.deviceID!=null">
                           `DeviceID`=#{model.deviceID},
                       </if>
        <if test="model.installed!=null">
                           `Installed`=#{model.installed},
                       </if>
        <if test="model.workState!=null">
                           `WorkState`=#{model.workState},
                       </if>
        <if test="model.status!=null">
                           `Status`=#{model.status},
                       </if>
        <if test="model.createrID!=null">
                           `CreaterID`=#{model.createrID},
                       </if>
        <if test="model.createTime!=null">
                           `CreateTime`=#{model.createTime},
                       </if>  
        </set>
        WHERE id=#{model.id}
    </update>

    <update id="delete" parameterType="java.lang.Integer" statementType="PREPARED"
            timeout="20">
        update DeviceInstallRecord set state=-1 WHERE id=#{id}
    </update>

    <insert id="insert" parameterType="com.fastrun.TempCollection.model.Deviceinstallrecord"
            flushCache="true"
            statementType="PREPARED"
            keyProperty="id"
            keyColumn=""
            useGeneratedKeys="true"
            timeout="20">
        INSERT INTO DeviceInstallRecord(`PositionID`,`DeviceID`,`Installed`,`WorkState`,`Status`,`CreaterID`,`CreateTime`) VALUES(#{model.positionID},#{model.deviceID},#{model.installed},#{model.workState},#{model.status},#{model.createrID},#{model.createTime})
    </insert>


    <select id="getCount" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT count(*) FROM DeviceInstallRecord
        <!--where>
            <if test="realName!=null and realName!=''">
                 realName like concat('%',#{realName},'%')
            </if>
            and state!=-1
        </where-->
    </select>
</mapper>
